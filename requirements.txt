# Local LoRA training for Clover Image Tiny.
# Pinned to the versions the published adapters were trained with.
accelerate==1.14.0
diffusers==0.39.0
transformers==5.14.1
datasets==5.0.1
huggingface-hub==1.26.0
safetensors==0.8.0
peft==0.20.0
pillow==12.3.0
tensorboard==2.21.0
PySide6-Essentials==6.11.1
# torch is intentionally not pinned here: install the build that matches your
# platform (CUDA / ROCm / Apple MPS / CPU) from https://pytorch.org/get-started.
torch>=2.7,<3
